# Real-time layer: Hocuspocus + Yjs persistence, auth hooks, update strategy

- **Label:** `wayfinder:research`
- **Status:** closed (resolved 2026-07-28)
- **Parent:** [0001 — MAP](./0001.md)
- **Blocking:** [0010](./0010.md) — feeds [0003](./0003.md)'s `page_docs` design
- **Assignee:** research subagent + ZCode

## Question

The stack is locked: **Yjs + Hocuspocus** as the real-time engine, **BlockNote** as the editor, one **Y-doc per page** persisted somehow, deployed as **long-lived processes** (Docker). This ticket resolves the **mechanics of the realtime layer** so a builder knows exactly what to wire. Questions to answer with research into current Hocuspocus/Yjs docs (not assumptions):

1. **Persistence adapter.** What is the recommended way to persist Y-docs in v2024+ Hocuspocus? Confirm whether to (a) store the binary doc in Postgres `bytea` via a custom `onStoreDocument`/`onChange` handler, (b) use the official Postgres/Redis/LevelDB extensions, or (c) a hybrid (updates streamed to a log, periodic snapshot). State the chosen approach with a link to the doc that supports it.
2. **Save cadence / batching.** Hocuspocus fires on every update. What's the recommended debounce/coalescing so we don't write to Postgres on every keystroke? What's the worst-case data-loss window if the server dies between snapshot saves?
3. **Authentication hook.** Confirm the exact hooks (`onAuthenticate`, `onListen`?) and what they receive — specifically how to read "which page doc" and "which user" from the connection context, so the effective-permission algorithm from ticket 0002 can gate access on connect *and* on load. Note any hook ordering pitfalls.
4. **Doc identity.** How does the client address a page's Y-doc (a doc name = page id / slug)? What's the recommended naming scheme and how does it map back to the `pages` row?
5. **Awareness/presence.** Out of the box, does Hocuspocus relay awareness (cursors, selections, who's-online) without extra wiring? Confirm so the product team knows whether live cursors are free or need work.
6. **Scaling ceiling.** What does Hocuspocus's single-process model mean for v1 scale? When does sticky-session / horizontal sharding by doc-name become necessary, and is that a v1 concern or safely post-v1?

**Resolve by:** `/research` subagent reading current Hocuspocus + Yjs docs and the BlockNote collaboration example. Output: a short findings doc (linked as an asset) answering each numbered question with a citation, plus a recommendation per item. This is AFK research — no human decision needed unless it surfaces a fork the map must escalate.

## Notes

- **Blocked by** nothing structural, but its *recommendation* on auth hooks (item 3) is consumed by the realtime-auth decision and must be consistent with ticket 0002's effective-permission algorithm.
- The persistence choice interacts with the data model (ticket 0003's `page_docs` table) — surface any dependency in the findings so the two tickets reconcile.

---

## Resolution — 2026-07-28

Resolved by research subagent. Full findings with citations at
[`../research/0004-realtime-layer.md`](../research/0004-realtime-layer.md).
Summary of the locked recommendations:

1. **Persistence** → official **`@hocuspocus/extension-database`**, writing raw
   `Y.encodeStateAsUpdate(doc)` bytes to a Postgres `page_docs.state bytea`
   column. No JSON, no `extension-postgres` (doesn't exist), and
   `extension-redis` is for *scaling*, not durability. **Feeds ticket 0003's
   `page_docs` table shape.**
2. **Batching** → keep Hocuspocus server defaults (`debounce: 2000ms`,
   `maxDebounce: 10000ms`); worst-case durable loss on a hard kill is ~10s,
   and clients self-heal on reconnect. **Add a graceful-shutdown flush.**
3. **Auth hooks** → do permission checks in **`onAuthenticate`** (not
   `onConnect`, which has no token/context). Return
   `{ user, pageId, role }` as connection context; set
   `connection.readOnly = true` for read-only roles. **No built-in periodic
   re-auth** → v1 live-revocation is reconnect-based (see ticket 0010).
4. **Doc identity** → name docs **`page:{pages.id UUID}`** (never the mutable
   slug). BlockNote's `withCollaboration` reuses the same name.
5. **Awareness/presence** → **free**: live cursors and presence work
   out-of-the-box via Yjs awareness relayed by Hocuspocus. (Product: live
   cursors are in v1 at no extra cost.)
6. **Scaling** → single Hocuspocus instance is fine for v1 (thousands of
   concurrent connections); add `extension-redis` + a second instance only
   when a concrete ceiling appears — safely **post-v1**.

### Dependencies surfaced
- **0003 (data model):** `page_docs(page_id UUID pk, state bytea, updated_at)`;
  the `pages.id` UUID is the doc name. `pages.title` likely lives in the
  relational row (reconcile with 0003's title-location decision).
- **0010 (realtime auth):** contract is `onAuthenticate(token) -> context
  {user, pageId, role}`; `role` drives `connection.readOnly`. Periodic
  re-auth is NOT available — 0010 must define v1 revocation (recommend
  reconnect-based, defer periodic re-check to post-v1).
