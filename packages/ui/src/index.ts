/**
 * @opennote/ui — shared React components (ticket 0009).
 *
 * Spine status: placeholder barrel. The sidebar, cmd+K palette, and block
 * components land with the editor ticket (0005) and search ticket (0008).
 * Components are added here as shared widgets so apps/web and any future
 * client consume one source.
 */
export const UI_PACKAGE_MARKER = "@opennote/ui";
