{
  "name": "@opennote/auth",
  "version": "0.1.0",
  "private": true,
  "description": "RBAC permission engine (the security boundary, ticket 0002) + Better Auth config (ticket 0006).",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./dist/index.js",
    "./engine": "./dist/engine.js",
    "./invites": "./dist/invites.js",
    "./better-auth": "./dist/better-auth.js",
    "./package.json": "./package.json"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.build.json",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "@opennote/db": "workspace:*",
    "@opennote/shared": "workspace:*",
    "better-auth": "^1.2.0"
  },
  "peerDependencies": {
    "@opennote/config": "workspace:*"
  },
  "devDependencies": {
    "@opennote/config": "workspace:*",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
