{
  "name": "@opennote/realtime",
  "version": "0.1.0",
  "private": true,
  "description": "Hocuspocus WebSocket server (separate long-lived process, ticket 0004/0009).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit -p tsconfig.build.json",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "@hocuspocus/server": "^2.13.5",
    "@hocuspocus/extension-database": "^2.13.5",
    "@opennote/auth": "workspace:*",
    "@opennote/config": "workspace:*",
    "@opennote/db": "workspace:*",
    "@opennote/shared": "workspace:*"
  },
  "devDependencies": {
    "@opennote/config": "workspace:*",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
